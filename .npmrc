# Use the public npm registry for this project instead of any user-level/private mirror.
# Do not add auth tokens here; keep secrets in your user/global npm config.
registry=https://registry.npmjs.org/
